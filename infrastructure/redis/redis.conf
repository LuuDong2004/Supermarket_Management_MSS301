# Minimal Redis config for local/dev. Tune for production (auth, persistence, memory).
appendonly yes
appendfsync everysec
maxmemory 256mb
maxmemory-policy allkeys-lru

# Set a password in real environments and pass it to clients via env:
# requirepass ${REDIS_PASSWORD}
