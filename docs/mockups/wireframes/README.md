# SMS low-fidelity wireframes

58 editable SVG mockups generated from the matching screenshots in `../screens`.

- `*.svg`: editable source wireframes.
- `png/*.png`: ready-to-preview 1600×1000 PNG renders.

The original screenshots are unchanged.
