# API base path. Requests go through the Vite dev proxy to the gateway.
VITE_API_URL=/api

# When true, the app uses the in-memory mock layer for services whose backend
# is not implemented yet (product, inventory, sales, supplier, reporting).
# Auth + user always try the real gateway first.
VITE_USE_MOCK=true
